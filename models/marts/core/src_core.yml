version: 2

models:
  - name: dim_customers
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
  - name: fct_orders
    columns:
      - name: customer_id
      #- name: order_id
        tests:
          - unique
          - not_null
  - name: int_orders_pivoted
    columns:
      - name: order_id
        tests:
          - unique
          - not_null
